---
title: "Model Catalog"
description: "Browse ZeroGPU models, compare pricing, and pick the model ID to call."
---

The **Model Catalog** lets you browse all available ZeroGPU models and compare **pricing** across the tasks you care about.

It’s useful when you’re selecting which `model` identifier to send to `POST /v1/responses`.

## Using a model from the catalog in an API request

Once you’ve picked a model card, copy the **Model ID** and set it in your request:

```json
{
  "model": "zlm-v1-iab-classify-edge"
}
```

The rest of the request (headers and `input`) follows the standard `POST /v1/responses` format.
