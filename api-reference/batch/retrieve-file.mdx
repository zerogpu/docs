---
title: "Retrieve file"
description: "Interactive playground for GET /v1/files/{file_id}."
openapi: "openapi/batch.openapi.json GET /v1/files/{file_id}"
---

Look up metadata for one file (`filename`, `bytes`, `purpose`, `status`) without downloading the body. Use [Download file](/api-reference/batch/download-file) for the JSONL content.

Full reference: [Files API](/docs/batch/files-api).
