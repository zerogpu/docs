---
title: "Create batch"
description: "Interactive playground for POST /v1/batches."
openapi: "openapi/batch.openapi.json POST /v1/batches"
---

Create a batch job from a JSONL file you already uploaded.

1. Upload input JSONL on [Upload file](/api-reference/batch/upload-file) or via [Quickstart](/docs/batch/getting-started) (`purpose=batch`).
2. Paste the returned `file-…` id into `input_file_id` below.
3. Set `endpoint` to `/v1/chat/completions` (must match every line in your JSONL).

Full reference: [Batches API](/docs/batch/batches-api).
